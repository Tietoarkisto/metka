package fi.uta.fsd.metka.storage.restrictions;

import fi.uta.fsd.metka.model.configuration.Target;

class ValueTargetHandler {
    static boolean handle(Target target) {
        return true;
    }
}
