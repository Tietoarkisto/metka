(function() {
    'use strict';
    MetkaJS.GUI.buttonHandlers['SAVE'] = (function() {
        function renderSaveButton(root, button) {

        }

        return {
            render: renderSaveButton
        }
    });
}());